# 认证命令 (auth)

## auth login - 登录

```bash
# 交互式登录
gc auth login

# 使用 Token 登录
gc auth login --token YOUR_TOKEN
```

## auth status - 查看认证状态

```bash
gc auth status
```

## auth token - 显示 Token

```bash
gc auth token
```

## auth logout - 登出

```bash
gc auth logout
```