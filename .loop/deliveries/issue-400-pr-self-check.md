## 作者自检

- 作者主体标识: AI 实现子代理 (glm-5.2 via opencode, 分支 bugfix/issue-400)
- 根因或实现理由: `os.WriteFile` 受进程 umask 影响（宽松 umask 下凭证文件可能 0666），对既有文件不收紧权限，且无法阻断 symlink 重定向攻击（含 Lstat→WriteFile 的 TOCTOU 竞态）。新增 `secureWriteFile` 用 Unix `O_NOFOLLOW` 原子打开 + fd `fchmod` 消除 TOCTOU，替换 `writeAuthState`/`writeConfigState` 中的裸 `os.WriteFile`。
- 主要修改: 5 文件 — config.go 移除 secureWriteFile + writeConfigState 补目录 Chmod；auth_config.go writeAuthState 补目录 Chmod；securewrite_unix.go 新增 O_NOFOLLOW 实现；securewrite_windows.go 新增 Lstat 实现；auth_config_test.go 新增 4 UT
  - `pkg/config/config.go`: 移除 secureWriteFile（拆到 build-tag 文件）；`writeConfigState` 补 `os.Chmod(configDir, 0o700)`；删未用 fmt import
  - `pkg/config/auth_config.go`: `writeAuthState` 补 `os.Chmod(configDir, 0o700)`
  - `pkg/config/securewrite_unix.go` (新, //go:build !windows): `O_NOFOLLOW` 原子打开 + fd `f.Chmod` + `f.Write`
  - `pkg/config/securewrite_windows.go` (新, //go:build windows): 保留 `Lstat`→`WriteFile`→`Chmod`（O_NOFOLLOW 不可移植）
  - `pkg/config/auth_config_test.go`: 新增 4 个 UT（拒 symlink / 权限硬化 / 新建文件权限 / 拒目录）
- 影响范围: `pkg/config` 包内 `writeAuthState`（`gc auth login` 写认证状态）与 `writeConfigState`（内部写配置状态）两条写入路径；不改变任何命令的对外行为。
- 单元测试: ✅ 4 个新增 UT 全绿，`-race` 通过
  - `TestSecureWriteFileRejectsSymlink` — symlink 拒绝（Unix O_NOFOLLOW + Windows Lstat），target 未被写入
  - `TestSecureWriteFileHardensPermissions` — 既有 0644 文件写后变 0600
  - `TestSecureWriteFileCreatesNewFileWithRestrictedPermissions` — 新建文件权限 0600
  - `TestSecureWriteFileRejectsDirectory` — path 是目录时失败
- 构建: ✅ `go build ./...` 全包通过；`go vet ./...` 无问题；`go vet -tags=system ./tests/system/` 系统测试代码编译通过
- 实际命令验证: ⏩ 跳过（用户指示）— 架构限制：`writeConfigState` 无 CLI 入口；`writeAuthState` 经 `gc auth login --with-token` 但 `login.go:137` 写前强制 `api.VerifyToken` 需真实 token+网络，规则禁止 AI 提供。待人工在 PR 评审时于 TTY 补做（清单见 issue-400-design.md）。
- 安全审查: ✅
  - 无硬编码 token/secret
  - UT 用 `"untouched"`/`"old"`/`"new"`/`"payload"` 假数据，无真实凭证
  - `secureWriteFile` 错误信息只含 path，不含 token
  - 设计文档人工验证清单用 `<真实token>` 占位符
  - 全程未调用 `gc auth token`/`gc auth status --show-token`，未读 `~/.config/gc/auth.json`，未打印 `GC_TOKEN`/`GITCODE_TOKEN`
  - 改动方向是收紧凭证文件权限 + 消除 TOCTOU，降低而非引入 token 泄露风险
  - 威胁模型假设已文档化（issue-400-design.md）：依赖 configDir 0700 防父目录/硬链接，不防同用户；Windows 保留 Lstat 是平台限制
- 文档同步: ✅ 设计文档 4 份（analysis/design/plan/record）已补到 `.loop/deliveries/`；本 PR 同步在 `spec/foundations/security.md` Token 管理章节新增"凭证文件落盘约束"（0600/0700/拒 symlink/O_NOFOLLOW/威胁模型）；命令行为不变，`docs/COMMANDS.md`/`README.md` 无需改。
- 风险: **high**（`classify-change-risk.py --base origin/main` → risk=high，代码触及 auth/token/config 高风险关键词，属安全改动）。按规范 high 风险独立 AI 评审后仍需人工最终确认。
- 未覆盖项: 真实命令验证（步骤 6）待人工在 PR 评审时补
- 自检结论: changes-requested 已修复（O_NOFOLLOW + spec 补约束 + 目录 Chmod + 边界 UT），可进入第二轮评审 → ready-for-review（high 风险，需独立 AI 评审 + 人工最终确认；真实命令验证作为评审前置项由人工补做）
