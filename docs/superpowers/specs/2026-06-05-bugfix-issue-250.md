# Fix #250: Issue Comments Auth Check Before Repo Validation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 `gc issue comments` 命令中 auth 检查在 repo 解析之前执行的问题，确保无效仓库参数返回清晰的用法错误而非混淆的认证错误。

**Architecture:** 纯代码块重排序——将 `parseRepo` 移到 `AuthenticatedClient` 之前，与 `pkg/cmd/pr/comments/comments.go` 已有的修复模式保持一致。零 API 变更，零新增依赖。

**Tech Stack:** Go 1.22+, Cobra, 标准库 `errors`

---

## 执行规则（本计划硬要求）

1. **每步完成后打印总结** — 每个 step 执行完毕后，必须在当前对话窗口输出该步骤的完成总结，包括：做了什么、结果如何、关键输出摘要
2. **所有远端操作必须附链接** — 涉及远端平台的操作（Issue、PR、CI Run），必须在总结中附带对应的 URL 链接
3. **禁止跳过链接** — 包括但不限于：
   - GitCode Issue: `https://gitcode.com/gitcode-cli/cli/issues/<N>`
   - GitCode PR: `https://gitcode.com/gitcode-cli/cli/pulls/<N>`
   - GitHub CI Run: `https://github.com/gitcode-cli/cli/actions/runs/<run-id>`

---

## Context

**问题来源:** [GitCode Issue #250](https://gitcode.com/gitcode-cli/cli/issues/250)

**根因:** `pkg/cmd/issue/comments/comments.go` 的 `commentsRun` 函数中，repo 解析和 auth 检查的顺序错误——先执行 `AuthenticatedClient`（auth），再执行 `ResolveRepo` + `parseRepo`（repo 校验）。当用户未配置 auth 且传入无效仓库参数时，得到的是混淆的认证错误，而非清晰的用法提示。

**已有修复模板:** `pkg/cmd/pr/comments/comments.go:82-87` 已在之前修复过相同问题，并保留了注释说明修复原因。

---

## 变更范围

**修改 1 个文件:**

| 文件 | 变更 | 说明 |
|------|------|------|
| `pkg/cmd/issue/comments/comments.go:84-104` | 重排序代码块 | 将 repo 解析移到 auth 检查之前 |

---

### Task 1: 重排序 commentsRun 中的代码块

**文件:** `pkg/cmd/issue/comments/comments.go`

- [ ] **Step 1: 修改 commentsRun 函数**

将 `commentsRun` 函数中第 84-104 行的代码块从:

```go
func commentsRun(opts *CommentsOptions) error {
	cs := opts.IO.ColorScheme()

	httpClient, err := opts.HttpClient()
	if err != nil {
		return fmt.Errorf("failed to create HTTP client: %w", err)
	}
	client, err := cmdutil.AuthenticatedClient(httpClient)
	if err != nil {
		return err
	}

	repository, err := cmdutil.ResolveRepo(opts.Repository, opts.BaseRepo)
	if err != nil {
		return err
	}

	owner, repo, err := parseRepo(repository)
	if err != nil {
		return err
	}
```

改为（跟随 `pkg/cmd/pr/comments/comments.go:82-87` 的模式）:

```go
func commentsRun(opts *CommentsOptions) error {
	cs := opts.IO.ColorScheme()

	// Validate repository input before auth so usage errors are not masked by
	// missing local credentials.
	repository, err := cmdutil.ResolveRepo(opts.Repository, opts.BaseRepo)
	if err != nil {
		return err
	}

	owner, repo, err := parseRepo(repository)
	if err != nil {
		return err
	}

	httpClient, err := opts.HttpClient()
	if err != nil {
		return fmt.Errorf("failed to create HTTP client: %w", err)
	}
	client, err := cmdutil.AuthenticatedClient(httpClient)
	if err != nil {
		return err
	}
```

- [ ] **Step 2: 运行现有测试确认无回归**

```bash
cd /home/wpf/claude-code/vibe-coding/cli
go test ./pkg/cmd/issue/comments/... -v
```

预期：所有测试 PASS（`TestNewCmdComments`, `TestCommentsRunFormatsTextOutput`, `TestCommentsRunJSONOutput`, `TestCommentsRunValidation`, `TestFormatID`）

- [ ] **Step 3: 运行完整构建**

```bash
cd /home/wpf/claude-code/vibe-coding/cli
go build -o ./gc ./cmd/gc
./gc version
```

预期：构建成功，`gc version` 输出当前版本号

- [ ] **Step 4: 运行完整测试套件**

```bash
cd /home/wpf/claude-code/vibe-coding/cli
go test ./...
```

预期：无新增失败

- [ ] **Step 5: Commit**

```bash
git add pkg/cmd/issue/comments/comments.go
git commit -m "fix(issue): validate repo before auth in issue comments command

Move ResolveRepo/parseRepo before AuthenticatedClient in issue
commentsRun so invalid repository arguments produce clear usage
errors instead of confusing auth errors.

The same fix was already applied to pr/comments/comments.go.

Fixes #250

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 后续步骤（遵循 AI 本地开发流程）

按 `spec/workflows/ai-local-development-workflow.md` 的管线逐步骤执行。**每步完成后必须输出总结到当前窗口，所有远端操作必须附带链接。**

### S1: 创建分支

```bash
git checkout -b bugfix/issue-250
```

**总结要求:** 打印分支名和当前工作树状态。

### S2: 本地验证

```bash
go test ./pkg/cmd/issue/comments/... -v
go build -o ./gc ./cmd/gc && ./gc version
go test ./...
```

**总结要求:** 打印每一步通过/失败数量，失败时附错误摘要。

### S3: 真实命令验证

```bash
gc issue comments 1 -R infra-test/gctest1
```

**总结要求:** 打印命令输出摘要，确认可正常列出 issue 评论。

### S4: 推送 + 创建 PR

```bash
git push -u origin bugfix/issue-250
gc pr create -R gitcode-cli/cli \
  --title "fix(issue): validate repo before auth in issue comments command" \
  --body "## 变更说明

将 \`gc issue comments\` 命令中 repo 解析移到 auth 检查之前。

## 关联 Issue

Fixes https://gitcode.com/gitcode-cli/cli/issues/250

## 变更范围

- \`pkg/cmd/issue/comments/comments.go\`: 重排序 repo 解析和 auth 检查顺序

## 自检

- [x] \`go test ./pkg/cmd/issue/comments/...\` 通过
- [x] \`go test ./...\` 无新增失败
- [x] \`go build\` 成功
- [x] 真实命令验证通过"
```

**⚠️ 必须打印 PR 链接:** `https://gitcode.com/gitcode-cli/cli/pulls/<N>`

### S5: 监控 CI

CI 随 PR 创建自动触发。监控 GitHub Actions:

```bash
gh run list --workflow=ci.yml --branch bugfix/issue-250 --limit 1
gh run watch <run-id>
gh run view <run-id> --json conclusion,name,url --jq '{name,conclusion,url}'
```

**⚠️ 必须打印 CI Run 链接:** `https://github.com/gitcode-cli/cli/actions/runs/<run-id>`

### S6: 安全审查

本次仅涉及代码块顺序调整，无新增依赖、无凭证/令牌操作。审查结论：**无安全风险**。

**总结要求:** 打印安全审查结论。

### S7: 文档同步

纯内部逻辑调整，不影响命令行为和用户接口，无需更新 `docs/COMMANDS.md` 或 `spec/`。

**总结要求:** 确认无需文档变更，打印确认。

### S8: 风险分级

```bash
python3 scripts/classify-change-risk.py
```

**总结要求:** 打印风险等级和判定依据。

### S9: PR 自检 + 标签更新

按 `docs/AI-TEMPLATES.md` pr-self-check 模板填写自检内容，然后更新标签:

```bash
# 更新 PR 标签（四维度：type + status + risk + scope）
gc pr edit <N> -R gitcode-cli/cli --labels "type/bug,scope/issue,risk/low,status/self-checked"

# 更新 issue 标签
gc issue label --add "status/in-progress" 250 -R gitcode-cli/cli
```

**⚠️ 必须打印 PR 和 Issue 链接确认。**

### S10: 独立评审

按 `spec/workflows/review-workflow.md` 启动团队评审。`risk/low` 变更可使用 AI 独立评审，4 个必审角色并行。

**总结要求:** 打印评审启动方式、各角色结论摘要和评审完成状态。

### S11: 合并

```bash
gc pr merge <N> -R gitcode-cli/cli
gc issue label --remove "status/in-progress" --add "status/merged" 250 -R gitcode-cli/cli
```

**⚠️ 必须打印合并确认和 Issue 链接:** `https://gitcode.com/gitcode-cli/cli/issues/250`

---

## 验证清单

- [ ] `go test ./pkg/cmd/issue/comments/... -v` 全部通过
- [ ] `go build -o ./gc ./cmd/gc` 构建成功
- [ ] `./gc version` 输出版本
- [ ] `go test ./...` 无新增失败
- [ ] 对真实仓库执行 `gc issue comments 1 -R infra-test/gctest1` 正常返回
- [ ] 无凭证/令牌泄露
- [ ] CI 全部通过（3 OS test + 3 OS build + lint + docker）
